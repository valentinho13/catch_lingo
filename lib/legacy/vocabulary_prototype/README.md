# Legacy Vocabulary Prototype

These files belong to the earlier vocabulary-app prototype:

- manual word list
- add-word form
- quiz-style training
- in-memory app state with simple local storage

They are not part of the current MVP navigation.

They are kept for reference because some ideas may later return as Dictionary or Review features, but the current CatchLingo direction is discovery and collection first.
